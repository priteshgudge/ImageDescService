// Filename: models/answer
define([
  'underscore',
  'backbone'
], function(_, Backbone){
  var ImageModel = Backbone.Model.extend({

  });
  // Return the model for the module
  return ImageModel;
});
