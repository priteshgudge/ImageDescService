// Filename: models/project
define([
  'underscore',
  'backbone'
], function(_, Backbone){
  var DynamicImage = Backbone.Model.extend({

  });
  // Return the model for the module
  return DynamicImage;
});
