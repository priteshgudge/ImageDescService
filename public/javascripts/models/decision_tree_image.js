// Filename: models/decision_tree_image.js
define([
  'underscore',
  'backbone'
], function(_, Backbone){
  var DecisionTreeImage = Backbone.Model.extend({

  });
  // Return the model for the module
  return DecisionTreeImage;
});
