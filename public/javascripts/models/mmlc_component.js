// Filename: models/mmlc_component
define([
  'underscore',
  'backbone'
], function(_, Backbone){
  var MmlcComponent = Backbone.Model.extend({

  });
  // Return the model for the module
  return MmlcComponent;
});
