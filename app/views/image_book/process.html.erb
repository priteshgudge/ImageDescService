<% @title="Update Book Image Descriptions" %>
<div class="container-fluid">
    <div class="row">
        <div class="col-md-2" role="navigation">
            <%= render partial: 'books/my_books_navigation' %>
        </div>
        <div class="col-md-10">
            <h1>Update Book Image Descriptions </h1>
            </br>
            <p>
            To insert image descriptions into a DAISY or EPUB book from the Bookshare library, follow these steps:
            </p>
            <ol>
            <li>Click the Browse (or Choose File) button below, and select a DAISY book (zip) or EPUB book (epub) file from your local drive to process.</li>
            <li>If the book is password protected, enter your password in the field.</li>
            <li>Click the Process button.</li>
            </ol>
            <p>
            Any existing image descriptions will be added to the book,
            and the book will be returned to you as a new file of the same format.
            </p> </br>

            <%= form_tag("/image_book/submit_to_get_descriptions", {:multipart => true}) do %>
            <p><label for="upload_file">Select book file</label> :
            <%= file_field_tag 'book' %> </p>
            <p><label for="upload_file">Enter password (if needed)</label> :
            <%= password_field_tag 'password' %> </p>
            <%= submit_tag "Process", :class => "btn btn-primary" %>
            <% end %>
        </div>
    </div>
 </div>